# Phase 10 独立复核发现登记（2026-09-22）

> 复核通道：`orca orchestration`（headless Orca runtime）。
> 复核代理：R1（治理数字复算）、R2（身份/审计/令牌绑定对抗式核验）。
> **边界**：两者均为**同框架只读核验代理**，**不等于**第三方独立审计（T36/T40 仍未闭环）。
> 所有结论绑定 `HEAD=aff8f2de5c904997143d57fe67a2e5079eddba3c`。

## 1. 复核通道修复（根因已确证）

Phase 9N / 9O / 9T 反复登记的「子代理通道失效、任务正文无法送达」，
本轮确证**根因是 Orca runtime 未运行**：`orca status` 返回
`runtimeReachable: false` / `runtimeState: stale_bootstrap`。

以 headless 方式启动后（`orca serve --port <port> --no-pairing`），
通道立即可用：探针代理成功回传结论，R1 / R2 均完整交付。

**处置**：后续独立复核统一经 `orca orchestration worker-start` + `check` 派发与回收，
不再依赖「任务书写盘 + 极短消息指路径」这一降级通道。

## 2. R1 治理数字独立复算（与主代理基线逐字一致）

| 项 | R1 实测 | 结论 |
|---|---|---|
| `git ls-files` tracked | 323 | 与提交 `aff8f2d` 绑定 |
| 禁用二进制扩展名命中 | 3（全部白名单思源黑体） | 违规 0 |
| 前端去重 `/api` 引用 | 189 | 一致 |
| 后端唯一路由路径 | 21（method+path 装饰器条目 27） | 一致 |
| 交集（前端调用且已实现） | 15 | 与 `KNOWN_IMPLEMENTED` 一致 |
| 缺口（前端调用但未实现） | 174 | 与 `KNOWN_UNIMPLEMENTED` 一致 |
| 同形字/零宽 token 级违规 | 0 | 扫描 277 文件 |

### 2.1 R1 证伪：tracked 数字口径过期

`docs/governance/TASKS.md` 与 `CLEANROOM-STATUS.md` 记载的「tracked 现值 293 / 275 / 269」
**已过期**，实测对应关系为：

| 记载值 | 实际对应提交 |
|---|---|
| 293 | `714414a` |
| 275 | `6c8ca98` |
| 269 | `9808bab` |

`tracked` 数随提交漂移，**必须以 commit 为锚**，不能作为「现值」长期引用。
**处置**：按既有治理先例「追加更正、不改写历史行」登记，不修改原历史行。

## 3. R2 身份/审计/令牌绑定对抗式核验

### 3.1 未被推翻的声明（证伪式抽查后仍成立）

| 编号 | 声明 | R2 结论 | 关键证据 |
|---|---|---|---|
| L1 | 401 错误码为纯 ASCII | **成立** | `errors.py:49` 与 `canvas-auth-401.json` 字节均为 `55 4E 41 55 54 48 4F 52 49 5A 45 44`，`isascii()=True`，全仓无西里尔/零宽污染 |
| L2 | 审计不落敏 | **成立（有范围限定）** | 字段白名单封闭，`id_token`/`code`/`state`/`nonce`/Cookie 无路径入账 |
| L3 | `azp` 严格比对客户端标识 | **成立** | 严格比对 `config.client_id`（独立于 audience）；多 audience 且缺 `azp` **明确拒绝** |
| L4 | 会话绝对过期不可绕过 | **成立** | `get_session()` 先判 `absolute_expires_at`，续期用 `min()` 封顶；3 条自建反例（含篡改 `expires_at`）均被拦截 |

### 3.2 R2 新发现（需入账，主代理已独立复验）

| 编号 | 严重度 | 发现 | 主代理复验 |
|---|---|---|---|
| P10-R-1 | 中（文档口径） | 401 错误码**大小写**在实现与文档间不一致：实现与黄金夹具为全大写 `UNAUTHORIZED`（12 字节），而 `AGENTS.md` §3.3 写为 `Unauthorized` | **已确证**：`AGENTS.md` 该处字节为 `556e617574686f72697a6564`（首字母大写），`docs/fixtures/canvas-auth-401.json` 与 `core/errors.py` 为全大写 |
| P10-R-2 | 中（证据可追溯） | 过度声称：`docs/governance/TASK-NOTES-2026-09-18.md:310` 称 `MAIN-PY-PHASE-D-DOCS-AUDIT.md` 已独立复核，但该文件**在仓内不存在** | **已确证**：全仓 `rglob` 无任何匹配文件 |
| P10-R-3 | 低（范围澄清） | 「不落敏」严格讲只对白名单**之外**字段成立；`subject`/`role`/`reason`/`auth_mode` 可被调用方传入任意字符串并原样落账进 logger（无「密钥格式」过滤器） | 接受；属调用点约束，全仓无实际错放调用 |

**处置**：

- P10-R-1：**不改实现、不改测试、不改夹具**（三者一致，改动将破坏黄金夹具与既有契约测试）。
  `AGENTS.md` 属**宪章级文档**，其修订需用户明确授权，本轮**不擅改**；
  本条以「追加登记 + 明确认定线上权威值为全大写 `UNAUTHORIZED`」方式收口。
- P10-R-2：如实登记为**证据链缺口**，保留原文不改写历史行；
  明确认定该处「已独立复核」表述**不成立**，不得作为发布授权依据。
- P10-R-3：作为**已知范围限定**登记，供后续审计方评估是否需要在调用点加格式过滤。

### 3.3 R2 附带观察（非本轮变更引入）

R2 观察到本地 `pytest` 为 `301 passed / 7 skipped`，
而 `CLEANROOM-STATUS.md` 登记的仍是 281 / 280 等历史快照；
差异由 Phase 10A 新增用例与**跨代理并发提交**共同造成。
**处置**：以 commit 为锚追加登记，不改写历史快照。

## 4. 本轮复核**未**覆盖的内容（不得外推）

- 未做真实多实例部署验证（本仓为单进程内存存储）。
- 未做真实生产 IdP 端到端验收（无真实 OP 与凭据）。
- 未做第三方独立审计（T36 / T40 仍未闭环）。
- 未复核 Phase 10B 观测阶段实现（该阶段在本复核之后才产出）。
