# 来源标记（审查前）

- 来源：旧仓库只读研究（`D:\Working\Code Pro\Gods-Workbench-release`，`main=31371df`）与公开文档
- 约束：无源码复制、无旧变量/函数/组件命名复制、无 DOM/CSS 结构复制、无用户数据
- 审核：尚未通过独立审核
- 状态：**NOT AUTHORIZED FOR PUBLIC DISTRIBUTION**

## 协议状态声明

本文件只形成**待审协议清单**，当前阶段**不实现任何连接器**、不发布任何可执行插件。

## 1. 协议清单（待审）

1. 插件身份与版本协商（插件 ID、插件版本、协议版本）
2. 协议兼容窗口（当前版本、最小支持版本、兼容到期日）
3. 回退策略（只读健康检查、兼容降级、阻断写入）
4. 稳定 ID 约束（`project_id` / `entity_id` / `canvas_id` / `asset_id` / `job_id`）
5. 写入门禁（认证、权限、目标范围、上下文一致性）
6. 权限最小化（网络白名单、文件访问申请制、禁止本地持久令牌）
7. 错误语义（`401/403/409/202` 与结构化错误体）
8. 审计与可追溯性（请求来源、协议版本、行为结果）

## 2. 非目标（Phase 2）

- 不定义插件运行时细节
- 不定义宿主 UI 集成细节
- 不实现连接器
- 不提供可安装包、脚本或测试桩

## 3. 进入实现前门禁（Phase 3 前）

1. 协议清单逐项冻结为版本化契约
2. 安全审查通过（鉴权、权限、敏感数据边界）
3. 兼容窗口与回退策略完成独立评审
4. 审计字段与保留策略完成合规确认
