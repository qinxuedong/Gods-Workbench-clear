# Gods-Workbench-clear 洁净实现章程

## 仓库定位

本仓库是 **Gods-Workbench-clear 的私有洁净实现仓**。当前发布状态为 **NOT AUTHORIZED FOR PUBLIC DISTRIBUTION**。不得提交图片、截图、音视频、用户数据、凭据、旧 `.git`、提交历史或未经审查的第三方资源；除 AGENTS.md 第 1 节第 2 条所列 3 个开源思源黑体（Source Han Sans CN，OFL-1.1）逐条精确路径白名单外，不得提交任何字体（白名单见 `AGENTS.md` §1.2）。

旧仓库不是本仓库的默认实现输入。用户本次明确授权的例外仅是：来源根目录 `D:\Working\Code Pro\Gods-Workbench-release` 中，能够证明属于用户且与画布完全无关的非画布代码切片。例外必须逐文件登记，不能把整仓库或整目录视为授权。

## 允许的信息来源

实现 `god-canvas`、项目中心和智能任务时，只能使用以下输入：

- 已复核的行为规范（`docs/behavior/`）
- 已复核的接口契约（`docs/contracts/`）
- 黄金夹具（`docs/fixtures/`）
- 公开依赖文档
- 经逐文件分类、哈希登记和用户授权确认的自有非画布切片

本轮分类结论见 `docs/provenance/CLEANROOM-CODE-CLASSIFICATION-2026-09-17.md`。当前仅接受日期选择器 JavaScript/CSS 两个非画布切片；其余旧静态文件不得作为整文件实现输入。

## 画布与直接依赖红线

无限画布、智能画布、画布资产桥接、生成提供商/适配器、Chrome 本地素材导入器、Photoshop UXP 连接器，以及无法证明与画布解耦的共享依赖，均禁止复制旧实现。它们只能依据行为规范、接口契约和黄金夹具重新设计 `god-canvas`。

`docs/behavior/PLUGIN-PROTOCOL-SPEC.md` 继续单独待审，严禁建立运行时依赖。

## 污染处置

发现未批准的旧源码、资源、用户数据或其衍生内容进入业务链路时，立即停止该阶段，将路径标为隔离，补充污染证明，并从最近洁净边界重写。不得把隔离内容复制回业务代码，也不得用测试通过替代来源审查。

## 门禁

每个阶段必须同时满足：输入来源可追溯、依赖闭包已分类、实现路径与契约一致、测试结果绑定当前文件快照。任何治理状态冲突、工作树未冻结或独立审查不可验证时，均不得发布、部署或声明授权。
